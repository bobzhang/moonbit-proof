predicate cauchy19_ok(
  a : Int,
  b : Int,
  c : Int,
  d : Int,
  e : Int,
  f : Int,
  g : Int,
  h : Int,
  i : Int,
  j : Int,
  k : Int,
  l : Int,
  m : Int,
  n : Int,
  o : Int,
  p : Int,
  q : Int,
  r : Int,
  s : Int,
  t : Int,
  u : Int,
  v : Int,
  w : Int,
  x : Int,
  y : Int,
  z : Int,
  aa : Int,
  ab : Int,
  ac : Int,
  ad : Int,
  ae : Int,
  af : Int,
  ag : Int,
  ah : Int,
  ai : Int,
  aj : Int,
  ak : Int,
  al : Int,
  result : Int
) {
  result ==
  (a * u - b * t) * (a * u - b * t) +
  (a * v - c * t) * (a * v - c * t) +
  (a * w - d * t) * (a * w - d * t) +
  (a * x - e * t) * (a * x - e * t) +
  (a * y - f * t) * (a * y - f * t) +
  (a * z - g * t) * (a * z - g * t) +
  (a * aa - h * t) * (a * aa - h * t) +
  (a * ab - i * t) * (a * ab - i * t) +
  (a * ac - j * t) * (a * ac - j * t) +
  (a * ad - k * t) * (a * ad - k * t) +
  (a * ae - l * t) * (a * ae - l * t) +
  (a * af - m * t) * (a * af - m * t) +
  (a * ag - n * t) * (a * ag - n * t) +
  (a * ah - o * t) * (a * ah - o * t) +
  (a * ai - p * t) * (a * ai - p * t) +
  (a * aj - q * t) * (a * aj - q * t) +
  (a * ak - r * t) * (a * ak - r * t) +
  (a * al - s * t) * (a * al - s * t) +
  (b * v - c * u) * (b * v - c * u) +
  (b * w - d * u) * (b * w - d * u) +
  (b * x - e * u) * (b * x - e * u) +
  (b * y - f * u) * (b * y - f * u) +
  (b * z - g * u) * (b * z - g * u) +
  (b * aa - h * u) * (b * aa - h * u) +
  (b * ab - i * u) * (b * ab - i * u) +
  (b * ac - j * u) * (b * ac - j * u) +
  (b * ad - k * u) * (b * ad - k * u) +
  (b * ae - l * u) * (b * ae - l * u) +
  (b * af - m * u) * (b * af - m * u) +
  (b * ag - n * u) * (b * ag - n * u) +
  (b * ah - o * u) * (b * ah - o * u) +
  (b * ai - p * u) * (b * ai - p * u) +
  (b * aj - q * u) * (b * aj - q * u) +
  (b * ak - r * u) * (b * ak - r * u) +
  (b * al - s * u) * (b * al - s * u) +
  (c * w - d * v) * (c * w - d * v) +
  (c * x - e * v) * (c * x - e * v) +
  (c * y - f * v) * (c * y - f * v) +
  (c * z - g * v) * (c * z - g * v) +
  (c * aa - h * v) * (c * aa - h * v) +
  (c * ab - i * v) * (c * ab - i * v) +
  (c * ac - j * v) * (c * ac - j * v) +
  (c * ad - k * v) * (c * ad - k * v) +
  (c * ae - l * v) * (c * ae - l * v) +
  (c * af - m * v) * (c * af - m * v) +
  (c * ag - n * v) * (c * ag - n * v) +
  (c * ah - o * v) * (c * ah - o * v) +
  (c * ai - p * v) * (c * ai - p * v) +
  (c * aj - q * v) * (c * aj - q * v) +
  (c * ak - r * v) * (c * ak - r * v) +
  (c * al - s * v) * (c * al - s * v) +
  (d * x - e * w) * (d * x - e * w) +
  (d * y - f * w) * (d * y - f * w) +
  (d * z - g * w) * (d * z - g * w) +
  (d * aa - h * w) * (d * aa - h * w) +
  (d * ab - i * w) * (d * ab - i * w) +
  (d * ac - j * w) * (d * ac - j * w) +
  (d * ad - k * w) * (d * ad - k * w) +
  (d * ae - l * w) * (d * ae - l * w) +
  (d * af - m * w) * (d * af - m * w) +
  (d * ag - n * w) * (d * ag - n * w) +
  (d * ah - o * w) * (d * ah - o * w) +
  (d * ai - p * w) * (d * ai - p * w) +
  (d * aj - q * w) * (d * aj - q * w) +
  (d * ak - r * w) * (d * ak - r * w) +
  (d * al - s * w) * (d * al - s * w) +
  (e * y - f * x) * (e * y - f * x) +
  (e * z - g * x) * (e * z - g * x) +
  (e * aa - h * x) * (e * aa - h * x) +
  (e * ab - i * x) * (e * ab - i * x) +
  (e * ac - j * x) * (e * ac - j * x) +
  (e * ad - k * x) * (e * ad - k * x) +
  (e * ae - l * x) * (e * ae - l * x) +
  (e * af - m * x) * (e * af - m * x) +
  (e * ag - n * x) * (e * ag - n * x) +
  (e * ah - o * x) * (e * ah - o * x) +
  (e * ai - p * x) * (e * ai - p * x) +
  (e * aj - q * x) * (e * aj - q * x) +
  (e * ak - r * x) * (e * ak - r * x) +
  (e * al - s * x) * (e * al - s * x) +
  (f * z - g * y) * (f * z - g * y) +
  (f * aa - h * y) * (f * aa - h * y) +
  (f * ab - i * y) * (f * ab - i * y) +
  (f * ac - j * y) * (f * ac - j * y) +
  (f * ad - k * y) * (f * ad - k * y) +
  (f * ae - l * y) * (f * ae - l * y) +
  (f * af - m * y) * (f * af - m * y) +
  (f * ag - n * y) * (f * ag - n * y) +
  (f * ah - o * y) * (f * ah - o * y) +
  (f * ai - p * y) * (f * ai - p * y) +
  (f * aj - q * y) * (f * aj - q * y) +
  (f * ak - r * y) * (f * ak - r * y) +
  (f * al - s * y) * (f * al - s * y) +
  (g * aa - h * z) * (g * aa - h * z) +
  (g * ab - i * z) * (g * ab - i * z) +
  (g * ac - j * z) * (g * ac - j * z) +
  (g * ad - k * z) * (g * ad - k * z) +
  (g * ae - l * z) * (g * ae - l * z) +
  (g * af - m * z) * (g * af - m * z) +
  (g * ag - n * z) * (g * ag - n * z) +
  (g * ah - o * z) * (g * ah - o * z) +
  (g * ai - p * z) * (g * ai - p * z) +
  (g * aj - q * z) * (g * aj - q * z) +
  (g * ak - r * z) * (g * ak - r * z) +
  (g * al - s * z) * (g * al - s * z) +
  (h * ab - i * aa) * (h * ab - i * aa) +
  (h * ac - j * aa) * (h * ac - j * aa) +
  (h * ad - k * aa) * (h * ad - k * aa) +
  (h * ae - l * aa) * (h * ae - l * aa) +
  (h * af - m * aa) * (h * af - m * aa) +
  (h * ag - n * aa) * (h * ag - n * aa) +
  (h * ah - o * aa) * (h * ah - o * aa) +
  (h * ai - p * aa) * (h * ai - p * aa) +
  (h * aj - q * aa) * (h * aj - q * aa) +
  (h * ak - r * aa) * (h * ak - r * aa) +
  (h * al - s * aa) * (h * al - s * aa) +
  (i * ac - j * ab) * (i * ac - j * ab) +
  (i * ad - k * ab) * (i * ad - k * ab) +
  (i * ae - l * ab) * (i * ae - l * ab) +
  (i * af - m * ab) * (i * af - m * ab) +
  (i * ag - n * ab) * (i * ag - n * ab) +
  (i * ah - o * ab) * (i * ah - o * ab) +
  (i * ai - p * ab) * (i * ai - p * ab) +
  (i * aj - q * ab) * (i * aj - q * ab) +
  (i * ak - r * ab) * (i * ak - r * ab) +
  (i * al - s * ab) * (i * al - s * ab) +
  (j * ad - k * ac) * (j * ad - k * ac) +
  (j * ae - l * ac) * (j * ae - l * ac) +
  (j * af - m * ac) * (j * af - m * ac) +
  (j * ag - n * ac) * (j * ag - n * ac) +
  (j * ah - o * ac) * (j * ah - o * ac) +
  (j * ai - p * ac) * (j * ai - p * ac) +
  (j * aj - q * ac) * (j * aj - q * ac) +
  (j * ak - r * ac) * (j * ak - r * ac) +
  (j * al - s * ac) * (j * al - s * ac) +
  (k * ae - l * ad) * (k * ae - l * ad) +
  (k * af - m * ad) * (k * af - m * ad) +
  (k * ag - n * ad) * (k * ag - n * ad) +
  (k * ah - o * ad) * (k * ah - o * ad) +
  (k * ai - p * ad) * (k * ai - p * ad) +
  (k * aj - q * ad) * (k * aj - q * ad) +
  (k * ak - r * ad) * (k * ak - r * ad) +
  (k * al - s * ad) * (k * al - s * ad) +
  (l * af - m * ae) * (l * af - m * ae) +
  (l * ag - n * ae) * (l * ag - n * ae) +
  (l * ah - o * ae) * (l * ah - o * ae) +
  (l * ai - p * ae) * (l * ai - p * ae) +
  (l * aj - q * ae) * (l * aj - q * ae) +
  (l * ak - r * ae) * (l * ak - r * ae) +
  (l * al - s * ae) * (l * al - s * ae) +
  (m * ag - n * af) * (m * ag - n * af) +
  (m * ah - o * af) * (m * ah - o * af) +
  (m * ai - p * af) * (m * ai - p * af) +
  (m * aj - q * af) * (m * aj - q * af) +
  (m * ak - r * af) * (m * ak - r * af) +
  (m * al - s * af) * (m * al - s * af) +
  (n * ah - o * ag) * (n * ah - o * ag) +
  (n * ai - p * ag) * (n * ai - p * ag) +
  (n * aj - q * ag) * (n * aj - q * ag) +
  (n * ak - r * ag) * (n * ak - r * ag) +
  (n * al - s * ag) * (n * al - s * ag) +
  (o * ai - p * ah) * (o * ai - p * ah) +
  (o * aj - q * ah) * (o * aj - q * ah) +
  (o * ak - r * ah) * (o * ak - r * ah) +
  (o * al - s * ah) * (o * al - s * ah) +
  (p * aj - q * ai) * (p * aj - q * ai) +
  (p * ak - r * ai) * (p * ak - r * ai) +
  (p * al - s * ai) * (p * al - s * ai) +
  (q * ak - r * aj) * (q * ak - r * aj) +
  (q * al - s * aj) * (q * al - s * aj) +
  (r * al - s * ak) * (r * al - s * ak)
}
