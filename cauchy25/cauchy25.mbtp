predicate cauchy25_ok(
  a : Int,
  b : Int,
  c : Int,
  d : Int,
  e : Int,
  f : Int,
  g : Int,
  h : Int,
  i : Int,
  j : Int,
  k : Int,
  l : Int,
  m : Int,
  n : Int,
  o : Int,
  p : Int,
  q : Int,
  r : Int,
  s : Int,
  t : Int,
  u : Int,
  v : Int,
  w : Int,
  x : Int,
  y : Int,
  z : Int,
  aa : Int,
  ab : Int,
  ac : Int,
  ad : Int,
  ae : Int,
  af : Int,
  ag : Int,
  ah : Int,
  ai : Int,
  aj : Int,
  ak : Int,
  al : Int,
  am : Int,
  an : Int,
  ao : Int,
  ap : Int,
  aq : Int,
  ar : Int,
  ba : Int,
  bb : Int,
  bc : Int,
  bd : Int,
  be : Int,
  bf : Int,
  result : Int
) {
  result ==
  (a * aa - b * z) * (a * aa - b * z) +
  (a * ab - c * z) * (a * ab - c * z) +
  (a * ac - d * z) * (a * ac - d * z) +
  (a * ad - e * z) * (a * ad - e * z) +
  (a * ae - f * z) * (a * ae - f * z) +
  (a * af - g * z) * (a * af - g * z) +
  (a * ag - h * z) * (a * ag - h * z) +
  (a * ah - i * z) * (a * ah - i * z) +
  (a * ai - j * z) * (a * ai - j * z) +
  (a * aj - k * z) * (a * aj - k * z) +
  (a * ak - l * z) * (a * ak - l * z) +
  (a * al - m * z) * (a * al - m * z) +
  (a * am - n * z) * (a * am - n * z) +
  (a * an - o * z) * (a * an - o * z) +
  (a * ao - p * z) * (a * ao - p * z) +
  (a * ap - q * z) * (a * ap - q * z) +
  (a * aq - r * z) * (a * aq - r * z) +
  (a * ar - s * z) * (a * ar - s * z) +
  (a * ba - t * z) * (a * ba - t * z) +
  (a * bb - u * z) * (a * bb - u * z) +
  (a * bc - v * z) * (a * bc - v * z) +
  (a * bd - w * z) * (a * bd - w * z) +
  (a * be - x * z) * (a * be - x * z) +
  (a * bf - y * z) * (a * bf - y * z) +
  (b * ab - c * aa) * (b * ab - c * aa) +
  (b * ac - d * aa) * (b * ac - d * aa) +
  (b * ad - e * aa) * (b * ad - e * aa) +
  (b * ae - f * aa) * (b * ae - f * aa) +
  (b * af - g * aa) * (b * af - g * aa) +
  (b * ag - h * aa) * (b * ag - h * aa) +
  (b * ah - i * aa) * (b * ah - i * aa) +
  (b * ai - j * aa) * (b * ai - j * aa) +
  (b * aj - k * aa) * (b * aj - k * aa) +
  (b * ak - l * aa) * (b * ak - l * aa) +
  (b * al - m * aa) * (b * al - m * aa) +
  (b * am - n * aa) * (b * am - n * aa) +
  (b * an - o * aa) * (b * an - o * aa) +
  (b * ao - p * aa) * (b * ao - p * aa) +
  (b * ap - q * aa) * (b * ap - q * aa) +
  (b * aq - r * aa) * (b * aq - r * aa) +
  (b * ar - s * aa) * (b * ar - s * aa) +
  (b * ba - t * aa) * (b * ba - t * aa) +
  (b * bb - u * aa) * (b * bb - u * aa) +
  (b * bc - v * aa) * (b * bc - v * aa) +
  (b * bd - w * aa) * (b * bd - w * aa) +
  (b * be - x * aa) * (b * be - x * aa) +
  (b * bf - y * aa) * (b * bf - y * aa) +
  (c * ac - d * ab) * (c * ac - d * ab) +
  (c * ad - e * ab) * (c * ad - e * ab) +
  (c * ae - f * ab) * (c * ae - f * ab) +
  (c * af - g * ab) * (c * af - g * ab) +
  (c * ag - h * ab) * (c * ag - h * ab) +
  (c * ah - i * ab) * (c * ah - i * ab) +
  (c * ai - j * ab) * (c * ai - j * ab) +
  (c * aj - k * ab) * (c * aj - k * ab) +
  (c * ak - l * ab) * (c * ak - l * ab) +
  (c * al - m * ab) * (c * al - m * ab) +
  (c * am - n * ab) * (c * am - n * ab) +
  (c * an - o * ab) * (c * an - o * ab) +
  (c * ao - p * ab) * (c * ao - p * ab) +
  (c * ap - q * ab) * (c * ap - q * ab) +
  (c * aq - r * ab) * (c * aq - r * ab) +
  (c * ar - s * ab) * (c * ar - s * ab) +
  (c * ba - t * ab) * (c * ba - t * ab) +
  (c * bb - u * ab) * (c * bb - u * ab) +
  (c * bc - v * ab) * (c * bc - v * ab) +
  (c * bd - w * ab) * (c * bd - w * ab) +
  (c * be - x * ab) * (c * be - x * ab) +
  (c * bf - y * ab) * (c * bf - y * ab) +
  (d * ad - e * ac) * (d * ad - e * ac) +
  (d * ae - f * ac) * (d * ae - f * ac) +
  (d * af - g * ac) * (d * af - g * ac) +
  (d * ag - h * ac) * (d * ag - h * ac) +
  (d * ah - i * ac) * (d * ah - i * ac) +
  (d * ai - j * ac) * (d * ai - j * ac) +
  (d * aj - k * ac) * (d * aj - k * ac) +
  (d * ak - l * ac) * (d * ak - l * ac) +
  (d * al - m * ac) * (d * al - m * ac) +
  (d * am - n * ac) * (d * am - n * ac) +
  (d * an - o * ac) * (d * an - o * ac) +
  (d * ao - p * ac) * (d * ao - p * ac) +
  (d * ap - q * ac) * (d * ap - q * ac) +
  (d * aq - r * ac) * (d * aq - r * ac) +
  (d * ar - s * ac) * (d * ar - s * ac) +
  (d * ba - t * ac) * (d * ba - t * ac) +
  (d * bb - u * ac) * (d * bb - u * ac) +
  (d * bc - v * ac) * (d * bc - v * ac) +
  (d * bd - w * ac) * (d * bd - w * ac) +
  (d * be - x * ac) * (d * be - x * ac) +
  (d * bf - y * ac) * (d * bf - y * ac) +
  (e * ae - f * ad) * (e * ae - f * ad) +
  (e * af - g * ad) * (e * af - g * ad) +
  (e * ag - h * ad) * (e * ag - h * ad) +
  (e * ah - i * ad) * (e * ah - i * ad) +
  (e * ai - j * ad) * (e * ai - j * ad) +
  (e * aj - k * ad) * (e * aj - k * ad) +
  (e * ak - l * ad) * (e * ak - l * ad) +
  (e * al - m * ad) * (e * al - m * ad) +
  (e * am - n * ad) * (e * am - n * ad) +
  (e * an - o * ad) * (e * an - o * ad) +
  (e * ao - p * ad) * (e * ao - p * ad) +
  (e * ap - q * ad) * (e * ap - q * ad) +
  (e * aq - r * ad) * (e * aq - r * ad) +
  (e * ar - s * ad) * (e * ar - s * ad) +
  (e * ba - t * ad) * (e * ba - t * ad) +
  (e * bb - u * ad) * (e * bb - u * ad) +
  (e * bc - v * ad) * (e * bc - v * ad) +
  (e * bd - w * ad) * (e * bd - w * ad) +
  (e * be - x * ad) * (e * be - x * ad) +
  (e * bf - y * ad) * (e * bf - y * ad) +
  (f * af - g * ae) * (f * af - g * ae) +
  (f * ag - h * ae) * (f * ag - h * ae) +
  (f * ah - i * ae) * (f * ah - i * ae) +
  (f * ai - j * ae) * (f * ai - j * ae) +
  (f * aj - k * ae) * (f * aj - k * ae) +
  (f * ak - l * ae) * (f * ak - l * ae) +
  (f * al - m * ae) * (f * al - m * ae) +
  (f * am - n * ae) * (f * am - n * ae) +
  (f * an - o * ae) * (f * an - o * ae) +
  (f * ao - p * ae) * (f * ao - p * ae) +
  (f * ap - q * ae) * (f * ap - q * ae) +
  (f * aq - r * ae) * (f * aq - r * ae) +
  (f * ar - s * ae) * (f * ar - s * ae) +
  (f * ba - t * ae) * (f * ba - t * ae) +
  (f * bb - u * ae) * (f * bb - u * ae) +
  (f * bc - v * ae) * (f * bc - v * ae) +
  (f * bd - w * ae) * (f * bd - w * ae) +
  (f * be - x * ae) * (f * be - x * ae) +
  (f * bf - y * ae) * (f * bf - y * ae) +
  (g * ag - h * af) * (g * ag - h * af) +
  (g * ah - i * af) * (g * ah - i * af) +
  (g * ai - j * af) * (g * ai - j * af) +
  (g * aj - k * af) * (g * aj - k * af) +
  (g * ak - l * af) * (g * ak - l * af) +
  (g * al - m * af) * (g * al - m * af) +
  (g * am - n * af) * (g * am - n * af) +
  (g * an - o * af) * (g * an - o * af) +
  (g * ao - p * af) * (g * ao - p * af) +
  (g * ap - q * af) * (g * ap - q * af) +
  (g * aq - r * af) * (g * aq - r * af) +
  (g * ar - s * af) * (g * ar - s * af) +
  (g * ba - t * af) * (g * ba - t * af) +
  (g * bb - u * af) * (g * bb - u * af) +
  (g * bc - v * af) * (g * bc - v * af) +
  (g * bd - w * af) * (g * bd - w * af) +
  (g * be - x * af) * (g * be - x * af) +
  (g * bf - y * af) * (g * bf - y * af) +
  (h * ah - i * ag) * (h * ah - i * ag) +
  (h * ai - j * ag) * (h * ai - j * ag) +
  (h * aj - k * ag) * (h * aj - k * ag) +
  (h * ak - l * ag) * (h * ak - l * ag) +
  (h * al - m * ag) * (h * al - m * ag) +
  (h * am - n * ag) * (h * am - n * ag) +
  (h * an - o * ag) * (h * an - o * ag) +
  (h * ao - p * ag) * (h * ao - p * ag) +
  (h * ap - q * ag) * (h * ap - q * ag) +
  (h * aq - r * ag) * (h * aq - r * ag) +
  (h * ar - s * ag) * (h * ar - s * ag) +
  (h * ba - t * ag) * (h * ba - t * ag) +
  (h * bb - u * ag) * (h * bb - u * ag) +
  (h * bc - v * ag) * (h * bc - v * ag) +
  (h * bd - w * ag) * (h * bd - w * ag) +
  (h * be - x * ag) * (h * be - x * ag) +
  (h * bf - y * ag) * (h * bf - y * ag) +
  (i * ai - j * ah) * (i * ai - j * ah) +
  (i * aj - k * ah) * (i * aj - k * ah) +
  (i * ak - l * ah) * (i * ak - l * ah) +
  (i * al - m * ah) * (i * al - m * ah) +
  (i * am - n * ah) * (i * am - n * ah) +
  (i * an - o * ah) * (i * an - o * ah) +
  (i * ao - p * ah) * (i * ao - p * ah) +
  (i * ap - q * ah) * (i * ap - q * ah) +
  (i * aq - r * ah) * (i * aq - r * ah) +
  (i * ar - s * ah) * (i * ar - s * ah) +
  (i * ba - t * ah) * (i * ba - t * ah) +
  (i * bb - u * ah) * (i * bb - u * ah) +
  (i * bc - v * ah) * (i * bc - v * ah) +
  (i * bd - w * ah) * (i * bd - w * ah) +
  (i * be - x * ah) * (i * be - x * ah) +
  (i * bf - y * ah) * (i * bf - y * ah) +
  (j * aj - k * ai) * (j * aj - k * ai) +
  (j * ak - l * ai) * (j * ak - l * ai) +
  (j * al - m * ai) * (j * al - m * ai) +
  (j * am - n * ai) * (j * am - n * ai) +
  (j * an - o * ai) * (j * an - o * ai) +
  (j * ao - p * ai) * (j * ao - p * ai) +
  (j * ap - q * ai) * (j * ap - q * ai) +
  (j * aq - r * ai) * (j * aq - r * ai) +
  (j * ar - s * ai) * (j * ar - s * ai) +
  (j * ba - t * ai) * (j * ba - t * ai) +
  (j * bb - u * ai) * (j * bb - u * ai) +
  (j * bc - v * ai) * (j * bc - v * ai) +
  (j * bd - w * ai) * (j * bd - w * ai) +
  (j * be - x * ai) * (j * be - x * ai) +
  (j * bf - y * ai) * (j * bf - y * ai) +
  (k * ak - l * aj) * (k * ak - l * aj) +
  (k * al - m * aj) * (k * al - m * aj) +
  (k * am - n * aj) * (k * am - n * aj) +
  (k * an - o * aj) * (k * an - o * aj) +
  (k * ao - p * aj) * (k * ao - p * aj) +
  (k * ap - q * aj) * (k * ap - q * aj) +
  (k * aq - r * aj) * (k * aq - r * aj) +
  (k * ar - s * aj) * (k * ar - s * aj) +
  (k * ba - t * aj) * (k * ba - t * aj) +
  (k * bb - u * aj) * (k * bb - u * aj) +
  (k * bc - v * aj) * (k * bc - v * aj) +
  (k * bd - w * aj) * (k * bd - w * aj) +
  (k * be - x * aj) * (k * be - x * aj) +
  (k * bf - y * aj) * (k * bf - y * aj) +
  (l * al - m * ak) * (l * al - m * ak) +
  (l * am - n * ak) * (l * am - n * ak) +
  (l * an - o * ak) * (l * an - o * ak) +
  (l * ao - p * ak) * (l * ao - p * ak) +
  (l * ap - q * ak) * (l * ap - q * ak) +
  (l * aq - r * ak) * (l * aq - r * ak) +
  (l * ar - s * ak) * (l * ar - s * ak) +
  (l * ba - t * ak) * (l * ba - t * ak) +
  (l * bb - u * ak) * (l * bb - u * ak) +
  (l * bc - v * ak) * (l * bc - v * ak) +
  (l * bd - w * ak) * (l * bd - w * ak) +
  (l * be - x * ak) * (l * be - x * ak) +
  (l * bf - y * ak) * (l * bf - y * ak) +
  (m * am - n * al) * (m * am - n * al) +
  (m * an - o * al) * (m * an - o * al) +
  (m * ao - p * al) * (m * ao - p * al) +
  (m * ap - q * al) * (m * ap - q * al) +
  (m * aq - r * al) * (m * aq - r * al) +
  (m * ar - s * al) * (m * ar - s * al) +
  (m * ba - t * al) * (m * ba - t * al) +
  (m * bb - u * al) * (m * bb - u * al) +
  (m * bc - v * al) * (m * bc - v * al) +
  (m * bd - w * al) * (m * bd - w * al) +
  (m * be - x * al) * (m * be - x * al) +
  (m * bf - y * al) * (m * bf - y * al) +
  (n * an - o * am) * (n * an - o * am) +
  (n * ao - p * am) * (n * ao - p * am) +
  (n * ap - q * am) * (n * ap - q * am) +
  (n * aq - r * am) * (n * aq - r * am) +
  (n * ar - s * am) * (n * ar - s * am) +
  (n * ba - t * am) * (n * ba - t * am) +
  (n * bb - u * am) * (n * bb - u * am) +
  (n * bc - v * am) * (n * bc - v * am) +
  (n * bd - w * am) * (n * bd - w * am) +
  (n * be - x * am) * (n * be - x * am) +
  (n * bf - y * am) * (n * bf - y * am) +
  (o * ao - p * an) * (o * ao - p * an) +
  (o * ap - q * an) * (o * ap - q * an) +
  (o * aq - r * an) * (o * aq - r * an) +
  (o * ar - s * an) * (o * ar - s * an) +
  (o * ba - t * an) * (o * ba - t * an) +
  (o * bb - u * an) * (o * bb - u * an) +
  (o * bc - v * an) * (o * bc - v * an) +
  (o * bd - w * an) * (o * bd - w * an) +
  (o * be - x * an) * (o * be - x * an) +
  (o * bf - y * an) * (o * bf - y * an) +
  (p * ap - q * ao) * (p * ap - q * ao) +
  (p * aq - r * ao) * (p * aq - r * ao) +
  (p * ar - s * ao) * (p * ar - s * ao) +
  (p * ba - t * ao) * (p * ba - t * ao) +
  (p * bb - u * ao) * (p * bb - u * ao) +
  (p * bc - v * ao) * (p * bc - v * ao) +
  (p * bd - w * ao) * (p * bd - w * ao) +
  (p * be - x * ao) * (p * be - x * ao) +
  (p * bf - y * ao) * (p * bf - y * ao) +
  (q * aq - r * ap) * (q * aq - r * ap) +
  (q * ar - s * ap) * (q * ar - s * ap) +
  (q * ba - t * ap) * (q * ba - t * ap) +
  (q * bb - u * ap) * (q * bb - u * ap) +
  (q * bc - v * ap) * (q * bc - v * ap) +
  (q * bd - w * ap) * (q * bd - w * ap) +
  (q * be - x * ap) * (q * be - x * ap) +
  (q * bf - y * ap) * (q * bf - y * ap) +
  (r * ar - s * aq) * (r * ar - s * aq) +
  (r * ba - t * aq) * (r * ba - t * aq) +
  (r * bb - u * aq) * (r * bb - u * aq) +
  (r * bc - v * aq) * (r * bc - v * aq) +
  (r * bd - w * aq) * (r * bd - w * aq) +
  (r * be - x * aq) * (r * be - x * aq) +
  (r * bf - y * aq) * (r * bf - y * aq) +
  (s * ba - t * ar) * (s * ba - t * ar) +
  (s * bb - u * ar) * (s * bb - u * ar) +
  (s * bc - v * ar) * (s * bc - v * ar) +
  (s * bd - w * ar) * (s * bd - w * ar) +
  (s * be - x * ar) * (s * be - x * ar) +
  (s * bf - y * ar) * (s * bf - y * ar) +
  (t * bb - u * ba) * (t * bb - u * ba) +
  (t * bc - v * ba) * (t * bc - v * ba) +
  (t * bd - w * ba) * (t * bd - w * ba) +
  (t * be - x * ba) * (t * be - x * ba) +
  (t * bf - y * ba) * (t * bf - y * ba) +
  (u * bc - v * bb) * (u * bc - v * bb) +
  (u * bd - w * bb) * (u * bd - w * bb) +
  (u * be - x * bb) * (u * be - x * bb) +
  (u * bf - y * bb) * (u * bf - y * bb) +
  (v * bd - w * bc) * (v * bd - w * bc) +
  (v * be - x * bc) * (v * be - x * bc) +
  (v * bf - y * bc) * (v * bf - y * bc) +
  (w * be - x * bd) * (w * be - x * bd) +
  (w * bf - y * bd) * (w * bf - y * bd) +
  (x * bf - y * be) * (x * bf - y * be)
}
